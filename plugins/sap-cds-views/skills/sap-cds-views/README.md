# sap-cds-views

CDS Views (Core Data Services) skill for Claude Code.

## Keywords

CDS, Core Data Services, define view, define view entity, define root view entity, view entity, root view entity, projection view, abstract entity, custom entity, association, join, composition, to parent, to one, to many, path expression, CDS annotation, AbapCatalog, AccessControl, Semantics, ObjectModel, Analytics, EndUserText, UI annotation, CDS function, CASE expression, CAST, COALESCE, CONCAT, currency conversion, unit conversion, virtual element, analytical view, cube view, dimension view, query view, CDS hierarchy, parent-child hierarchy, metadata extension, EXTEND VIEW, DCL, data control language, pfcg_auth, access control, S/4HANA CDS, BTP ABAP CDS, EWM CDS, TM CDS, VDM, virtual data model, CDS test double, SELECT from CDS, CDS where-used, ADT data preview
